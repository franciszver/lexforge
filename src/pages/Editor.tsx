import React from 'react';

/**
 * Editor Page
 * 
 * The core workspace featuring the TipTap editor and the RAG suggestion sidebar.
 */
export const Editor: React.FC = () => {
    return (
        <div className="page-container">
            <h1>Editor Workspace</h1>
        </div>
    );
};
