import React from 'react';

/**
 * Admin Console Page
 * 
 * Allows administrators to manage templates, users, and view system analytics.
 */
export const Admin: React.FC = () => {
    return (
        <div className="page-container">
            <h1>Admin Console</h1>
        </div>
    );
};
